#include <iostream>
#include <fstream>
#include <string>

// Функция для создания папки
void create_folder(const std::string& folder_name) {
    std::string command = "mkdir " + folder_name; // Создаем команду для создания папки с помощью команды "mkdir"
    system(command.c_str()); // Запускаем команду в командной строке
}

// Функция для создания файла
void create_file(const std::string& file_name) {
    std::ofstream file(file_name); // Открываем поток для записи в файл с указанным именем
    file.close(); // Закрываем файл
}

int main() {
    std::string folder_prefix = "folder_"; // Префикс для названия папок
    std::string file_prefix = "file_"; // Префикс для названия файлов
    
    int num_folders = 5; // Количество папок для создания
    int num_files_per_folder = 3; // Количество файлов, которые нужно создать в каждой папке
    
    for (int i = 1; i <= num_folders; i++) { // Цикл по созданию папок
        std::string folder_name = folder_prefix + std::to_string(i); // Генерируем название папки
        create_folder(folder_name); // Создаем папку с указанным именем
        
        for (int j = 1; j <= num_files_per_folder; j++) { // Цикл по созданию файлов в каждой папке
            std::string file_name = folder_name + "/" + file_prefix + std::to_string(j) + ".txt"; // Генерируем название файла в формате "папка/файл.txt"
            create_file(file_name); // Создаем файл с указанным именем в текущей папке
        }
    }

    return 0;
}


Код выше состоит из следующих частей:

1. Подключение необходимых заголовочных файлов.
2. Определение функции create_folder, которая принимает название папки в качестве параметра и использует команду "mkdir" в системной командной строке для создания папки.
3. Определение функции create_file, которая принимает название файла в качестве параметра. Она открывает поток для записи в файл с заданным именем и немедленно его закрывает, создавая пустой файл.
4. В функции main определены переменные folder_prefix и file_prefix, которые задают префиксы для названий папок и файлов соответственно.
5. Определены переменные num_folders и num_files_per_folder, указывающие количество папок, которые нужно создать, и количество файлов, которые нужно создать в каждой папке.
6. Цикл for выполняет определенное количество итераций в соответствии с num_folders для создания папок. Внутри цикла генерируется название папки, вызывается функция create_folder для создания папки и затем выполняется вложенный цикл.
7. Во вложенном цикле for создается определенное количество файлов в каждой папке. Путь к файлу генерируется с использованием названия текущей папки, определенного префикса для файла и номера файла. Затем вызывается функция create_file для создания файла с указанным именем в текущей папке.
8. Когда все папки и файлы были созданы, программа завершается.

Примечание: Предоставленный код создает папки и файлы в текущем рабочем каталоге.
